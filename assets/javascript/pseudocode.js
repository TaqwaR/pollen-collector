//Crystal Collector Game

  //Game objective: collect enough crystals to match randomly chosen number

//global variables:

  //var crystalOne
  //var crystalTwo
  //var crystalThree
  //var crystalFour

  //var numberGoal - random number between 19 and 120 (for number goal)
  //var crystalNumber - random between 1 and 12 (for each clickable crystal)
  //var totalPoints - counter (store numbers for each click). Push numbers from clicks and store here.

  //var winCounter
  //var lossCounter


//global functions

    //on click - add X numbers to the total points counter
    //function reset() { --
      //resets value of each crystal,
      //resets total points to 0,
      //generates new random number
    //}


//if statements

    //*if* total points = random number --
      //run reset function
      //display "you win" message
      //increment the winCounter up by 1

    //*else if* total points > random number --
      //run reset functions
      //display "you lose" message
      //increment the loseCounter up by 1
